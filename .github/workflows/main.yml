name: GitHub Actions Demo
on:
  workflow_dispatch:
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        uses: sudosubin/git-clone-action@v1
        with:
        repository: 'padavan/rt-n56u.git'
        platform: 'bitbucket.org'
